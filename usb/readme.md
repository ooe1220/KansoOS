簡素OSの土台を借りながら、USBメモリドライバを開発します。
現在簡素OSはATAドライバしか開発していないので実機で起動してもディスク読み込みをすると固まります。
USBドライバの開発は長期戦になることが予想されるため、一旦簡素OSとは分けて開発し、完成してからOSの方に組み込みます。

実行する場合は一旦`./build.sh`を実行し、関連ファイルをビルドしてから`./usb.sh`を実行します。
```
./build.sh
./usb.sh
```
